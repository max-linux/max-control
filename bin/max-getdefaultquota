#!/bin/sh
#
# really uggly hack to read gconf data from EBOX
#
#

DEF=$(grep '"userquota_size"' /var/lib/ebox/gconf/ebox/modules/samba/GeneralSettings/%gconf.xml | awk -F '"' '{print $8}')

if [ "DEF" = "" ]; then
  echo 500
else
 echo $DEF
fi
